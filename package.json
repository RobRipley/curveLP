{
  "name": "curve-lp-analyzer",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "cd client && npm install && npm run build && mkdir -p ../dist && cp -r build/* ../dist/",
    "dev": "wrangler pages dev --proxy 5001 -- npm start",
    "start": "node server/server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "ethers": "^6.13.4",
    "graphql": "^16.8.1",
    "graphql-request": "^6.1.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}